.wrapper-highlight {
  position: relative;
  height: auto; }

.wrapper-article {
  position: relative;
  padding: 50px 0;
  margin-top: 70px;
  border-top: 1px solid #eaeaea;
  border-bottom: 1px solid #eaeaea; }

.article-header {
  display: flex;
  height: auto; }
  .article-header h4.article-show-title {
    text-align: center;
    vertical-align: middle;
    line-height: 400px;
    font-family: 'Cinzel', serif; }

.wrapper-article-half {
  position: relative;
  display: -ms-flexbox;
  display: flex;
  -ms-flex-pack: center;
  justify-content: center;
  width: 50%;
  flex-direction: column; }

.article-show-img {
  object-fit: cover;
  width: 488px;
  height: 460px;
  border-radius: 6px; }

@media (max-width: 1140px) {
  .wrapper-article {
    padding: 110px 0;
    margin-top: 0px; }

  .article-header {
    -ms-flex-direction: column;
    flex-direction: column; }
    .article-header h4.article-show-title {
      line-height: 50px;
      padding-bottom: 20px; }

  .article-show-img {
    width: 100% !important; }

  .wrapper-article-half {
    width: 100%; }

  .article-container {
    -ms-flex-direction: column;
    flex-direction: column; } }
@media only screen and (max-width: 767px) and (orientation: portrait) {
  .wrapper-highlight {
    padding-top: 10px; }

  .article-header h4.article-show-title {
    font-size: 22px;
    line-height: 30px;
    padding-top: 10px;
    padding-bottom: 0; }

  .article-show-img {
    height: 250px; }

  .wrapper-article {
    padding: 0;
    margin-top: 30px; } }
